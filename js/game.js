let config={
	width=800,
	height=800,
	scene:{
		preload:preload,
		create:create,
		update:update
	}
};
let game = new Phaser.Game(config);
function preload() {
//this.load.image("background","")
}
function create() {
	// body...
}
function update(argument) {
	// body...
}